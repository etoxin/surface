/- kdl-version 2

surface "0.1"

application "clickCounter" {
    purpose "Reject unreleased behaviour syntax."
}

behaviour "increment" {
    context "Increase the counter by 1."
}

interface "clickCounter" {
    context "Render a click counter."
}

screen "home" {
    use (interface)"clickCounter"
}
