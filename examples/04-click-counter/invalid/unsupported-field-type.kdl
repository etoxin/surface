/- kdl-version 2

surface "0.1"

application "clickCounter" {
    purpose "Reject an unsupported field type."
}

collection "counter" {
    (promise)"value"
}

interface "clickCounter" {
    context "Render a click counter."
}

screen "home" {
    use (interface)"clickCounter"
}
