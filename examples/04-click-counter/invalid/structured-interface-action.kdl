/- kdl-version 2

surface "0.1"

application "clickCounter" {
    purpose "Reject structured interface actions."
}

interface "clickCounter" {
    context "Render a counter starting at 0."
    button "Increment"
}

screen "home" {
    use (interface)"clickCounter"
}
