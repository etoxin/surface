/- kdl-version 2

surface "0.1"

application "contactViewer" {
    purpose "Reject the former entity declaration."
}

entity "contact" {
    (string)"id"
}

interface "contactViewer" {
    context "Render a contact."
}

screen "contact" {
    use (interface)"contactViewer"
}
