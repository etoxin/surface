/- kdl-version 2

surface "0.1"

application "contactViewer" {
    purpose "Reject the former entity reference annotation."
}

collection "contact" {
    (string)"id"
}

interface "contactViewer" {
    context (entity)"contact" "Render a contact."
}

screen "contact" {
    use (interface)"contactViewer"
}
